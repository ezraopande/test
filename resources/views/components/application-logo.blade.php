@if ($settings->logo)
                           
                           <img src="{{ asset('storage/' . $settings->logo) }}" alt="Site Logo" class="img-fluid mt-2" width="70">
                       @endif